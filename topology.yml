name: blue-team-sandbox

hosts:
  - name: wazuh-manager
    base_box: debian-10-x86_64
    flavor: medium
    mgmt_user: debian
    mgmt_protocol: ssh
    networks:
      - monitoring-net

  - name: grafana
    base_box: debian-10-x86_64
    flavor: small
    mgmt_user: debian
    mgmt_protocol: ssh
    networks:
      - monitoring-net

  - name: win-host1
    base_box: windows-10
    flavor: small
    mgmt_user: Administrator
    mgmt_protocol: winrm
    networks:
      - internal-net

  - name: win-host2
    base_box: windows-10
    flavor: small
    mgmt_user: Administrator
    mgmt_protocol: winrm
    networks:
      - internal-net

  - name: linux-host1
    base_box: debian-10-x86_64
    flavor: small
    mgmt_user: debian
    mgmt_protocol: ssh
    networks:
      - internal-net

  - name: linux-host2
    base_box: debian-10-x86_64
    flavor: small
    mgmt_user: debian
    mgmt_protocol: ssh
    networks:
      - internal-net

networks:
  - name: internal-net
    cidr: 10.0.0.0/24
